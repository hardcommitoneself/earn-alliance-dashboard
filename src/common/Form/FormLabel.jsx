import React from "react";
import * as EA from "./styles";

const FormLabel = (props) => {
  const { children } = props;
  return <EA.FormLabel>{children}</EA.FormLabel>;
};

export default FormLabel;
